export const photos = {
    TON: {
        confirmed: process.env.TON_SUCCESS,
        failed: process.env.TON_FAILED
    },
    USDT: {
        confirmed: process.env.USDT_SUCCESS,
        failed: process.env.USDT_FAILED
    }
}
